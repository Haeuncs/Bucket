
def default_pods
  pod 'SwiftGen'
  pod 'SnapKit' 
end

target 'Bucket (iOS)' do
  use_frameworks!
  platform :ios, '9.0'
  default_pods
end

target 'Bucket (macOS)' do
  use_frameworks!
  platform:'osx','10.15'
  default_pods
end
